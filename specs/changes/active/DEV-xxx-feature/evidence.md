# 验证证据

本文件只记录可复验事实，不用计划结果代替实际结果。

## 基线

- 分支：`<分支>`
- Commit：`<完整 commit>`
- 运行环境：`<操作系统、运行时和关键工具版本>`
- 工作区状态：`<clean 或已说明的差异>`

## 自动化结果

| 命令 | 结果 | 证据位置 |
| --- | --- | --- |
| `<命令>` | `<退出码与摘要>` | `<CI、日志或 ArtifactRef>` |

## 人工或 live 结果

记录场景 ID、执行时间、环境、脱敏运行标识、结果和证据位置。未执行的场景必须明确标为未验证。

## 需求追踪

| 需求 ID | 验收场景 | 实现位置 | 测试或证据 | 结论 |
| --- | --- | --- | --- | --- |
| `<需求 ID>` | `<场景 ID>` | `<路径>` | `<测试或证据>` | `<结论>` |

## 未验证边界

列出当前证据不能证明的能力、风险和外部依赖。

## 最终结论

记录完成、部分完成、失败或阻塞结论及其依据。只有全部通过条件满足后才能宣称完成。
