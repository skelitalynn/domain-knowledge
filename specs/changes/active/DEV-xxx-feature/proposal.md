# 变更提案

> 将本目录复制并重命名为真实的 `DEV-<编号>-<简短名称>` 后使用。

## 元数据

- 状态：Draft
- 负责人：`<负责人>`
- 目标版本：`<版本或里程碑>`
- 关联需求：`<需求 ID>`
- 关联议题或 PR：`<链接或编号>`

## 背景与问题

说明当前问题、受影响的使用者，以及为什么需要现在解决。

## 目标

描述本次变更完成后可观察、可验证的结果。

## 非目标

列出本次明确不处理的能力，防止实现范围扩张。

## 范围

说明受影响的产品行为、领域、接口、数据、安全边界和文档。

## 主要风险

记录失败模式、兼容性影响和需要提前验证的假设。
