# 将本文件复制为 .env.local，并把占位值替换成本地密钥。
# 不要提交 .env.local。
WP_KNOWLEDGE_WRITE_TOKEN=replace-with-a-long-random-token
WP_FLYWHEEL_HOME=.workpanel
# 可选：把知识摄取和扫描根目录指向 wpKnowledge 的本地检出。
WP_KNOWLEDGE_REPOSITORY=../wpKnowledge
# 可选：仅允许来源注册访问这些 HTTPS host，逗号分隔；默认拒绝所有远程 host。
# WP_SOURCE_ALLOWED_HOSTS=docs.example.com,raw.githubusercontent.com
# HTTPS 来源的 credentialRef 使用 secret://env/变量名，只在服务端解析对应环境变量。
# WP_SOURCE_TOKEN=replace-with-a-source-access-token
# 可选：Pi Agent 的单次输出上限与上下文窗口；上下文窗口必须不小于输出上限。
# WP_PI_MAX_TOKENS=32768
# WP_PI_CONTEXT_WINDOW=128000
# 可选：Pi Agent 遇到空输出或 Schema 不合法输出时的独立会话尝试总数，范围 1..3。
# WP_PI_MAX_SCHEMA_ATTEMPTS=2
# 可选：启用公司 CodeAgent CLI。CLI 必须支持 auth status --json，以及 run 的
# --non-interactive/--output jsonl/--role/--session/--model/--tools 参数；Prompt 只从 stdin 读取。
# WP_FLYWHEEL_AGENT_PROVIDER=company-codeagent-cli
# WP_CODEAGENT_BIN=codeagent
# WP_CODEAGENT_RUN_ARGS_JSON=["run"]
# WP_CODEAGENT_MODEL=company-default
# WP_CODEAGENT_TIMEOUT_MS=600000
# WP_CODEAGENT_AUTH_TIMEOUT_MS=15000
# WP_CODEAGENT_MAX_OUTPUT_BYTES=2097152
# WP_CODEAGENT_ALLOWED_ROOTS=/absolute/path/to/repository
