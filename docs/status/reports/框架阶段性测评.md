# LangGraph 多 Agent 框架阶段性测评

> 本文是阶段性验证报告；最新项目进度以 [`../../DEVELOPMENT-STATUS.md`](../../DEVELOPMENT-STATUS.md) 为准。

## 结论

当前可以汇报为：**框架机制已通过确定性验收，真实 Agent 效果尚未评测，生产可用性尚未评测。**

这意味着 LangGraph 选型已被证明能够承载本项目当前的固定七 Agent 拓扑、并发分支、迭代、Checkpoint 恢复、取消和确定性 Gate；公司 CodeAgent CLI Adapter 后续虽已完成协议接线，但在公司环境 live Run 与真实任务集尚未建立之前，仍不能声称知识质量、代码质量、成本、吞吐量或生产稳定性已经达标。

## 测评边界

```text
本阶段可以证明                         本阶段不能证明
──────────────────────────────────    ──────────────────────────────
七个 Agent 都在图中并实际被调度         真实模型生成内容是否足够好
输入输出契约会 fail closed              公司 CodeAgent CLI 是否稳定
Run 配置在启动时被冻结                  并发容量、耗时、成本是否达标
并行、循环、取消和 Checkpoint 可工作     敌对代码是否能被安全执行
基础设施失败不会交给 ReviewAgent         真实业务知识是否可直接发布
只有确定性 PASS 可以发布
```

因此，本报告评价的是“框架能不能正确承载 Agent”，不是“Agent 本身聪不聪明”。

## 自动化基线

从仓库根目录执行：

```bash
npm run evaluate:framework
```

命令输出一份 JSON 报告，任一关键检查失败时退出码为非零。它不调用公网模型、不要求 CodeAgent 登录，也不写入现有 HTTP API。

| 维度 | 验证效果 | 证据 | 当前口径 |
|---|---|---|---|
| DDD 边界 | Domain、Application、Infrastructure、Interface 依赖方向没有倒置 | `tests/contract/architecture.test.ts` | 可验证 |
| 七 Agent 拓扑 | Orchestrator、DocWorker、DocGen、TestGen、Code、Check、Review 均保留并执行 | `tests/integration/langgraph-infrastructure.test.ts`、`tests/acceptance/automated-langgraph-flow.test.ts` | 可验证 |
| 输入输出契约 | 非法 `AgentCommand` 在 Provider 前失败；下游只接收通过 Schema 的 `AgentResult` | `tests/integration/agent-contracts.test.ts` | 可验证 |
| 公司 CodeAgent CLI 接缝 | 七角色、认证、stdin、JSON/JSONL、session、取消与错误边界保持稳定 | `tests/integration/company-codeagent-cli.test.ts` | 协议可验证；live 未评测 |
| 配置复现 | Provider、模型、参数摘要、Prompt revision、工具及 Schema URI/内容摘要在 Run 启动时冻结；不兼容恢复会 fail closed | `tests/integration/run-configuration.test.ts` | 可验证 |
| 编排机制 | 并发、循环、取消、失败恢复和 Checkpoint 重放符合固定图规则 | `tests/integration/langgraph-infrastructure.test.ts` | 可验证 |
| 治理闭环 | Fixture 能经历失败、修订、重新生成、独立评测和确定性发布 | `tests/acceptance/automated-langgraph-flow.test.ts` | 可验证，但不代表真实模型质量 |
| 真实 Agent 质量 | 正确率、完整率、幻觉率、人工采纳率 | 尚无真实任务集与 CodeAgent Run | 未评测 |
| 生产运行 | P95 时延、吞吐、认证续期、限流、并发冲突、长期稳定性 | 尚无公司环境压测 | 未评测 |

## 汇报建议

建议把当前状态分成三句话：

1. **选型合理性**：LangGraph 已覆盖固定拓扑、并行、循环、Checkpoint 和恢复需求，且被自动化测试锁定；当前没有证据表明需要更换编排框架。
2. **框架可用性**：使用确定性 Fixture 时，七个 Agent 的工作关系、契约、评测和发布闭环能够完整运行，框架层验收通过。
3. **效果待验证项**：真实 Agent 的生成效果以及公司环境的性能、成本和稳定性仍需在 CodeAgent Adapter 接入后用 live Run 单独测量，不能用协议夹具或 Fixture 结果代替。

## 下一阶段指标

接入公司 CodeAgent 后，保持本报告的框架基线不变，再增加真实效果指标：

- 任务成功率：完整跑到确定性 Gate 的 Run 占比；
- 一次通过率：无需 `ITERATE` 即达到 PASS 的比例；
- 人工采纳率：生成文档或修订被审核人接受的比例；
- 契约失败率：Provider 原始输出无法规范化为 `AgentResult` 的比例；
- 恢复成功率：超时或进程失败后从 Checkpoint 恢复成功的比例；
- 时延与资源：各节点 P50/P95 时延、CLI 进程峰值内存和并发上限；
- 成本代理：公司侧可取得的调用量、token 或配额消耗指标。

这些指标不要求修改现有前台或 HTTP API。若以后需要在 Console 展示框架测评或真实 Agent 指标，应由前台/API 负责人先定义新增只读接口，本后台分支不预先扩展响应结构。
