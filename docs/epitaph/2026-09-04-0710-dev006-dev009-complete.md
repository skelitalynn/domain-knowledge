# DEV-006～DEV-009 完成交接

## 目标

记录 Console B2～B4 与公司 CodeAgent CLI Adapter 完成后的可核验主线状态，并为 DEV-010 的公司环境 live 验收提供边界。

## 已核验状态

- PR #10 已合并 DEV-006，merge commit 为 `9f19a3e`。
- PR #11 已合并 DEV-007/008，merge commit 为 `3a6135e`。
- PR #12 已合并 DEV-009，merge commit 为 `dbfdf26`；CI `verify` 成功。
- DEV-006～008 聚合分支在 Node 24.13.0 下通过测试 135/135、站点契约 12/12、Chromium E2E 13/13、框架测评 6/6 `ACCEPTED`。
- DEV-009 新增 `CompanyCodeAgentCliAdapter`：认证预检、stdin Prompt、JSON/JSONL、七角色固定工具、角色工作区、0600 session、超时/AbortSignal、进程组终止、稳定错误分类、Schema/角色失败关闭和脱敏审计均有协议夹具覆盖。
- DEV-009 在 Node 24.13.0 下通过 TypeScript、Spec（17 schemas、7 commands、8 results、51 P0）、测试 142/142、站点契约 12/12、框架测评 7/7 `ACCEPTED`。
- 框架报告对公司 CodeAgent 的结论为 `CONTRACT_VERIFIED_LIVE_NOT_EVALUATED`；当前环境没有安装 `codeagent`，未伪造 live Run、模型质量、容量或稳定性结论。
- DEV-009 未修改 `web/`、`site/`、前台设计或现有 HTTP 路由/响应。

## 约束与未决事项

1. DEV-010 必须在公司已登录环境核对真实 CLI 的参数与 JSONL 协议；若公司版本与 `.env.example` 的 Adapter 协议不同，只调整 Infrastructure Adapter/部署配置，不改变 Domain/Application、固定七 Agent 拓扑或现有 HTTP API。
2. live 验收需要覆盖七角色完整闭环、认证续期、模型不可用、超时/取消、session 恢复、Schema 失败率、P50/P95、吞吐和人工采纳率；协议夹具不能替代这些证据。
3. `code` 角色仍只允许看到固定 commit 生成的公开接口视图；公司 CLI 的工具限制必须实测，不能只依据 argv 声明安全。
4. 敌对代码执行安全、四点崩溃注入与完整权限拒绝审计仍分别属于 DEV-012/后续安全验收，不因 DEV-009 完成而关闭。
5. 主工作区原有未跟踪 `AGENTS.md` 与 `docs/epitaph/2026-09-04-0624-latest-progress.md` 没有被覆盖；远端主线已纳管一个多一行中文摘要的 `AGENTS.md`，同步前应先人工处理本地未跟踪文件冲突。

## 推荐下一步

- 以 `dbfdf26` 后的最新 `origin/main` 新建 DEV-010 worktree。
- 先执行 `codeagent auth status --json` 和一个不含业务数据的最小协议探测，确认公司 CLI 实际 flags、最终事件字段、session 语义和凭据过期行为。
- 协议一致后运行固定七角色真实闭环，归档脱敏 run/session 关联 ID 与效果/容量指标；不合格时保留 `CONTRACT_VERIFIED_LIVE_NOT_EVALUATED`，不得把 DEV-010 标为 Done。

## 入口

- 项目状态：[`docs/DEVELOPMENT-STATUS.md`](../DEVELOPMENT-STATUS.md)
- Adapter：[`src/infrastructure/agents/company-codeagent/index.ts`](../../src/infrastructure/agents/company-codeagent/index.ts)
- 契约测试：[`tests/integration/company-codeagent-cli.test.ts`](../../tests/integration/company-codeagent-cli.test.ts)
