# DEV-007 / DEV-008 端到端验收与交接

- 验收结论：`Accepted with follow-ups`
- 验收时间：2026-09-04 02:23 CST
- 工作分支：`codex/dev007-provider-observability`
- 叠加基线：`codex/dev006-action-center-runs`（PR #10）
- 交付 PR：[linlisWorkTeam/domain-knowledge#11](https://github.com/linlisWorkTeam/domain-knowledge/pull/11)
- 最终验收代码：`f0dccd1`
- 动图捕获代码：`fd1fb55`；后续 `f0dccd1` 只增加同一 UI 的焦点竞态保护与稳定测试，不改变画面结构

## 交付范围

DEV-007 已完成 Provider 状态、配置与验证接口，API Key 加密持有与脱敏读取，公开 HTTPS、DNS 固定和禁重定向边界，以及 Pi Agent 官方 SDK 七节点执行路径。空输出、无效 JSON 和 Schema 错误使用全新会话做有界尝试；模型调用重试与工作流节点恢复分别计量。Run 快照冻结 Provider 协议、地址、模型、Token/上下文上限和 Schema 尝试上限的摘要，任何非秘密执行参数变化都会拒绝恢复。

运行观测已覆盖批次、节点和排队 P50/P95、Provider 调用、两类重试、Token、可空成本、首次修订通过率、三轮收敛率、人工介入比例、平均处理时间与短期复发率。历史无法证明 `readyAt` 的节点不进入排队样本；无可信定价源时成本保持 `null`/`—`。脱敏运行报告同时汇总 DSH JSONL 与 SQLite 中的 Pi 调用事实，不导出 Prompt、正文、路径、模型配置正文或凭据。

DEV-008 已完成知识血缘与版本差异、评测列表/详情/受控证据下载/规则修订、FILE 与 HTTPS 来源注册/刷新/启停/漂移，以及 Knowledge Health。来源漂移与不可用会形成持久化 SOURCE Action Item；远程来源只允许显式 allowlist 中的公开 HTTPS 地址，拒绝查询、fragment、凭据、私网或混合 DNS 和重定向，并限制为 10 MiB。历史无法证明规则 revision 的评测明确返回 `UNBOUND`，不伪造绑定。

控制台保持已冻结的七页结构：“操作中心、飞轮批次、知识、工作流图、评测、来源、Agent 设置”。用户状态与原因采用中文，`Agent` 保留为批准术语；设置页显示实际执行 Provider，不把 fixture 伪装为 Pi。`GET /api/v1/system/status` 与知识目录默认状态的文档也已按真实响应重新对齐。

## 自动化验收

统一使用 Node `v24.13.0`：

| 门禁 | 最终结果 |
|---|---|
| `npm run typecheck` | 通过 |
| `npm run validate:specs` | 通过：17 schemas、7 commands、8 results、51 P0 |
| `npm test` | 135/135 通过 |
| `npm run test:ui` | Chromium 13/13 通过 |
| `npm run test:architecture` | 6/6 通过 |
| `npm run site:check` | 12/12 通过 |
| `npm run evaluate:framework` | 6/6，结论 `ACCEPTED` |
| `npm audit --json` | 0 vulnerabilities |
| `git diff --check` | 通过 |

新增的关键验收包括：

- Pi 快照在 `WP_PI_MAX_TOKENS`、`WP_PI_MAX_SCHEMA_ATTEMPTS` 或上下文窗口变化时失败关闭，DSH Token 配置变化不会误伤 Pi；
- HTTPS Source 使用无公网 MockAgent 验证 200 内容摘要、302 不跟随、每次访问只做一次批准解析，并把获批地址交给受控 dispatcher；
- Provider 密钥保存、失败验证和成功启用全程不回填、不泄漏；
- 来源漂移从来源页回流到操作中心，处理动作仍经过 revision、幂等和审计；
- Knowledge 血缘/差异可反向进入版本、批次和评测，评测规则生成不可变新修订；
- 异步刷新知识列表时，抽屉关闭会按版本 ID 恢复键盘焦点；关联的前四项浏览器序列重复执行 3 轮，12/12 通过。

曾出现的非最终结果也保留说明：首次把后台全量测试与浏览器测试并行运行时，七页双主题测试超过原 30 秒预算；该跨 14 个页面/主题状态的用例改为明确 60 秒预算后单独通过。随后完整 UI 复跑暴露知识列表异步替换导致的焦点竞态，修复并增加稳定等待后，最终完整结果为 13/13。

## 视觉验收

固定像素基线为 [`tests/e2e/console.spec.ts-snapshots/action-center-1363x936-light-linux.png`](../../tests/e2e/console.spec.ts-snapshots/action-center-1363x936-light-linux.png)，尺寸 `1363 × 936`，SHA-256 为 `e6b68d1d830ea02cf0c3751faa83018fb06aeb9e15471c6b0249cf773e7d6e88`。基线重新生成后人工检查确认：顶部栏为 103 px 且标题/操作垂直居中；全局基准字号 14 px；微软雅黑优先且不使用宋体作为主要字体；一个页面只有一个 H1；治理状态显示“待处理/已接手/已解决”，不再直接显示英文状态码。

浏览器验收逐页检查七个页面的亮色与暗色主题：成功 API 状态下没有错误或 Partial 提示，亮色主题不存在已知暗色专用表面。另行通过 `390 × 844` 移动导航与 `640 × 450` 视口下的 200% 页面缩放核心路径。

Site 已直接嵌入 [`site/console-dev007-dev008.gif`](../../site/console-dev007-dev008.gif)，并为减少动态效果的访问者提供 [`site/console-dev007-dev008-poster.webp`](../../site/console-dev007-dev008-poster.webp)：

- GIF：8 帧、`960 × 600`，SHA-256 `da655beb047538dab017dc60b32edef682a14e6cfeb5abd1ca15871d21c5df00`；
- Poster：`960 × 600`，SHA-256 `94efed5b64b2b8efedb1a8efb0a2aba459b11ef9259a04f64d04cf9e1419db1a`；
- 证据批次：`96f86f71-2868-424e-bef6-5ba8b507698b`；
- 帧序列：操作中心、知识血缘、知识差异、评测与规则、来源漂移、Agent 设置与运行观测、治理效果、浅色主题；
- 已人工查看全部 8 帧，未见亮色残留暗色表面、重复 Graph、额外页面副标题或中英文状态混杂。

动图由实际 `createKnowledgeServer` 提供的 Console 录制，知识、评测、来源、Provider 配置、节点和运营调用事实分别通过正式 API、Workflow Observer 与 SQLite 指标存储写入；治理事项通过正式 HTTP 命令接手和解决。录制使用确定性本地验收种子与受控上游，不代表外部模型的生产效果；录制时密码输入为空，`secretRendered=false`。

## 尚未由本次验收证明

- 外部生产 Provider 的内容质量、吞吐、延迟、价格和长期可用性；当前框架测评明确把这些项标为 `NOT_EVALUATED`。
- 公司 CodeAgent CLI 接入；这是下一阶段 DEV-009。
- 敌对代码执行的完整安全隔离与生产容量。
- Provider 加密设置文件与 SQLite 命令回执之间的 crash-exactly-once；进程在两个提交之间崩溃时可能以 revision 冲突结束，留待 DEV-012。
- 权限拒绝类安全事件全部投影成 Action Item；`KF-SYS-033` 剩余部分留待 DEV-012。
- Preview 当前是权限 `0600` 的本机 AES-256-GCM 秘密持有，不等同于企业 KMS。
- Correction 关系仍由知识版本 metadata 投影，尚未建成独立领域聚合。
- 左上角项目空间选择与跨项目隔离属于 F6 后续范围。

## 后续建议

PR #11 当前叠加在仍开放的 PR #10 上，应先合入 #10，再复核或重定向 #11 的 base 后合入。下一开发项按 `docs/DEVELOPMENT-STATUS.md` 执行 DEV-009；上述未证明项不能因为本次 `Accepted with follow-ups` 被表述为生产已完成。
